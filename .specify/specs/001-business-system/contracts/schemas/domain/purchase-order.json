{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://idap.local/schemas/domain/purchase-order.json",
    "title": "PurchaseOrder",
    "type": "object",
    "required": [
        "id",
        "supplierId",
        "status",
        "items"
    ],
    "properties": {
        "id": {
            "$ref": "../common/id.json"
        },
        "supplierId": {
            "$ref": "../common/id.json"
        },
        "warehouseId": {
            "$ref": "../common/id.json"
        },
        "status": {
            "type": "string",
            "enum": [
                "DRAFT",
                "SUBMITTED",
                "APPROVED",
                "RECEIVED",
                "CANCELLED"
            ]
        },
        "orderDate": {
            "type": "string",
            "format": "date-time"
        },
        "items": {
            "type": "array",
            "minItems": 1,
            "items": {
                "type": "object",
                "required": [
                    "productId",
                    "quantity"
                ],
                "properties": {
                    "productId": {
                        "$ref": "../common/id.json"
                    },
                    "quantity": {
                        "type": "number"
                    },
                    "price": {
                        "$ref": "../common/money.json"
                    },
                    "warehouseId": {
                        "$ref": "../common/id.json"
                    }
                },
                "additionalProperties": false
            }
        },
        "total": {
            "$ref": "../common/money.json",
            "readOnly": true
        },
        "notes": {
            "type": "string"
        }
    },
    "additionalProperties": false
}
# 文档总览

当前优先：先完成进销存后台业务系统，智能问数在其之上二期扩展。

本目录保留全局规约；专题规格（spec/plan/tasks/contracts）集中在 `docs/specs/` 下：

- 全局规约：
  - `docs/constitution.md` — 项目宪章（规约与护栏）
- 专题规格：
  - `docs/specs/001-business-system/` — 进销存业务系统（当前优先）
  - `docs/specs/002-intelligent-querying/` — 智能问数（后续阶段）
  - `docs/specs/000-project/` — 可选的项目横向事项（如仍需保留）

命名规则：文档使用数字前缀升序排序（两位或三位）。

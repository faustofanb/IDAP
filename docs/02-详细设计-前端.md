# IDAP 详细设计 - 前端

## 1. 技术栈

| 技术             | 版本  | 用途                     |
| ---------------- | ----- | ------------------------ |
| **Vue**          | 3.4+  | 渐进式 JavaScript 框架   |
| **Element Plus** | 2.5+  | 企业级 Vue 3 UI 组件库   |
| **Pinia**        | 2.1+  | Vue 官方状态管理库       |
| **Vue Router**   | 4.2+  | Vue 官方路由管理器       |
| **Vite**         | 5.0+  | 下一代前端构建工具       |
| **Bun**          | 1.0+  | 高性能 JavaScript 运行时 |
| **ECharts**      | 5.5+  | 数据可视化图表库         |
| **Axios**        | 1.6+  | HTTP 请求库              |
| **Markdown-it**  | 14.0+ | Markdown 解析器          |
| **Highlight.js** | 11.9+ | 代码语法高亮             |
| **Day.js**       | 1.11+ | 轻量级时间处理库         |

---

## 2. 项目结构

```
frontend/
├── public/                     # 静态资源
│   └── favicon.ico
├── src/
│   ├── assets/                 # 资源文件
│   │   ├── styles/
│   │   │   └── main.scss       # 全局样式
│   │   └── images/
│   ├── components/             # 通用组件
│   │   ├── common/             # 公共组件
│   │   │   ├── Header.vue
│   │   │   ├── Sidebar.vue
│   │   │   └── Loading.vue
│   │   └── chat/               # 聊天相关组件
│   │       ├── ChatInput.vue   # 消息输入框
│   │       ├── ChatMessage.vue # 消息展示
│   │       └── ChartViewer.vue # 图表查看器
│   ├── views/                  # 页面视图
│   │   ├── HomePage.vue        # 首页
│   │   ├── ChatPage.vue        # 对话页
│   │   ├── HistoryPage.vue     # 历史记录
│   │   ├── LoginPage.vue       # 登录页
│   │   ├── AdminPage.vue       # 管理后台
│   │   └── NotFoundPage.vue    # 404 页面
│   ├── router/                 # 路由配置
│   │   └── index.js
│   ├── stores/                 # Pinia 状态管理
│   │   ├── user.js             # 用户状态
│   │   ├── chat.js             # 对话状态
│   │   └── config.js           # 系统配置
│   ├── services/               # API 服务
│   │   ├── api.js              # Axios 封装
│   │   └── sse.js              # SSE 客户端
│   ├── utils/                  # 工具函数
│   │   ├── date.js             # 时间格式化
│   │   ├── storage.js          # LocalStorage 封装
│   │   └── validators.js       # 表单验证
│   ├── App.vue                 # 根组件
│   └── main.js                 # 入口文件
├── index.html                  # HTML 模板
├── vite.config.js              # Vite 配置
├── package.json                # 依赖管理
├── .env                        # 环境变量
├── .env.production             # 生产环境变量
└── README.md
```

---

## 3. 核心功能设计

### 3.1 路由设计

```javascript
// router/index.js
const routes = [
  {
    path: "/",
    name: "home",
    component: () => import("@/views/HomePage.vue"),
    meta: { requiresAuth: false },
  },
  {
    path: "/chat",
    name: "chat",
    component: () => import("@/views/ChatPage.vue"),
    meta: { requiresAuth: true },
  },
  {
    path: "/history",
    name: "history",
    component: () => import("@/views/HistoryPage.vue"),
    meta: { requiresAuth: true },
  },
  {
    path: "/admin",
    name: "admin",
    component: () => import("@/views/AdminPage.vue"),
    meta: { requiresAuth: true, requiresAdmin: true },
  },
  {
    path: "/login",
    name: "login",
    component: () => import("@/views/LoginPage.vue"),
  },
  {
    path: "/:pathMatch(.*)*",
    name: "not-found",
    component: () => import("@/views/NotFoundPage.vue"),
  },
];
```

**路由守卫**:

```javascript
router.beforeEach((to, from, next) => {
  const userStore = useUserStore();

  if (to.meta.requiresAuth && !userStore.isAuthenticated) {
    next({ name: "login", query: { redirect: to.fullPath } });
  } else if (to.meta.requiresAdmin && userStore.user?.role !== "admin") {
    next({ name: "home" });
  } else {
    next();
  }
});
```

---

### 3.2 状态管理 (Pinia)

#### 3.2.1 User Store (stores/user.js)

```javascript
export const useUserStore = defineStore("user", () => {
  const user = ref(null);
  const token = ref(localStorage.getItem("token") || "");

  const isAuthenticated = computed(() => !!token.value);
  const isAdmin = computed(() => user.value?.role === "admin");

  async function login(username, password) {
    const response = await api.post("/auth/login", { username, password });
    token.value = response.data.token;
    user.value = response.data.user;
    localStorage.setItem("token", token.value);
  }

  function logout() {
    user.value = null;
    token.value = "";
    localStorage.removeItem("token");
  }

  return { user, token, isAuthenticated, isAdmin, login, logout };
});
```

#### 3.2.2 Chat Store (stores/chat.js)

```javascript
export const useChatStore = defineStore("chat", () => {
  const sessions = ref([]);
  const currentSessionId = ref(null);
  const messages = ref([]);
  const isLoading = ref(false);

  async function createSession(title = "新对话") {
    const response = await api.post("/sessions", { title });
    sessions.value.unshift(response.data);
    currentSessionId.value = response.data.session_id;
  }

  async function sendMessage(question) {
    isLoading.value = true;
    messages.value.push({ role: "user", content: question });

    const response = await api.post("/query", {
      question,
      session_id: currentSessionId.value,
    });

    messages.value.push({
      role: "assistant",
      content: response.data.answer,
      sql: response.data.sql,
      chart: response.data.chart,
    });

    isLoading.value = false;
  }

  return { sessions, messages, isLoading, createSession, sendMessage };
});
```

---

### 3.3 API 服务设计

#### 3.3.1 Axios 封装 (services/api.js)

```javascript
const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || "http://localhost:8080/api/v1",
  timeout: 30000,
});

// 请求拦截器：添加 JWT Token
api.interceptors.request.use((config) => {
  const userStore = useUserStore();
  if (userStore.token) {
    config.headers.Authorization = `Bearer ${userStore.token}`;
  }
  return config;
});

// 响应拦截器：处理错误
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      const userStore = useUserStore();
      userStore.logout();
      router.push("/login");
    }
    return Promise.reject(error);
  }
);
```

#### 3.3.2 SSE 客户端 (services/sse.js)

```javascript
export class SSEClient {
  constructor(url, options = {}) {
    this.url = url;
    this.options = options;
    this.eventSource = null;
  }

  connect(onMessage, onError, onComplete) {
    const queryString = new URLSearchParams(this.options.params).toString();
    const fullUrl = `${this.url}?${queryString}`;

    this.eventSource = new EventSource(fullUrl);

    this.eventSource.onmessage = (event) => {
      const data = JSON.parse(event.data);
      if (data.type === "done") {
        onComplete?.(data);
        this.close();
      } else {
        onMessage?.(data);
      }
    };

    this.eventSource.onerror = (error) => {
      onError?.(error);
      this.close();
    };
  }

  close() {
    if (this.eventSource) {
      this.eventSource.close();
      this.eventSource = null;
    }
  }
}
```

---

### 3.4 核心组件设计

#### 3.4.1 ChatInput 组件

**功能**:

- 多行文本输入（自动调整高度）
- Cmd/Ctrl + Enter 快捷发送
- 禁用状态（发送中）

**Props**:

```javascript
{
    placeholder: String,
    disabled: Boolean
}
```

**Events**:

```javascript
{
    send: (text: string) => void
}
```

#### 3.4.2 ChatMessage 组件

**功能**:

- 用户消息：纯文本显示
- AI 消息：Markdown 渲染 + SQL 高亮 + 图表展示 + 来源引用

**Props**:

```javascript
{
    message: {
        role: 'user' | 'assistant',
        content: string,
        sql?: string,
        chart?: object,
        sources?: Array<{ title: string, url: string }>
    }
}
```

#### 3.4.3 ChartViewer 组件

**功能**:

- ECharts 图表渲染
- 响应式尺寸
- 图表下载

**Props**:

```javascript
{
  config: object; // ECharts 配置对象
}
```

---

### 3.5 Element Plus 集成

#### 3.5.1 按需导入配置

```javascript
// vite.config.js
import { ElementPlusResolver } from "unplugin-vue-components/resolvers";

export default defineConfig({
  plugins: [
    AutoImport({
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],
});
```

#### 3.5.2 常用组件

**表单组件**:

- `el-form`, `el-form-item` - 表单容器
- `el-input`, `el-textarea` - 输入框
- `el-select`, `el-option` - 下拉选择
- `el-button` - 按钮

**反馈组件**:

- `el-message` - 消息提示
- `el-notification` - 通知
- `el-loading` - 加载状态
- `el-dialog` - 对话框

**布局组件**:

- `el-container`, `el-header`, `el-main`, `el-aside` - 布局容器
- `el-card` - 卡片容器
- `el-divider` - 分割线

**数据展示**:

- `el-table` - 表格
- `el-pagination` - 分页
- `el-tag` - 标签
- `el-avatar` - 头像

---

## 4. 样式设计

### 4.1 主题变量 (assets/styles/main.scss)

```scss
:root {
  // 主色调
  --primary-color: #667eea;
  --primary-hover: #5568d3;
  --primary-light: #f0f2ff;

  // 背景色
  --bg-primary: #ffffff;
  --bg-secondary: #f7f8fc;
  --bg-hover: #e9ecef;

  // 文字色
  --text-primary: #2d3748;
  --text-secondary: #718096;

  // 边框色
  --border-color: #e2e8f0;

  // 状态色
  --success-color: #48bb78;
  --warning-color: #ed8936;
  --error-color: #f56565;
}

[data-theme="dark"] {
  --primary-color: #7c3aed;
  --bg-primary: #1a202c;
  --bg-secondary: #2d3748;
  --text-primary: #f7fafc;
  --text-secondary: #cbd5e0;
  --border-color: #4a5568;
}
```

### 4.2 响应式断点

```scss
$breakpoints: (
  xs: 480px,
  sm: 768px,
  md: 1024px,
  lg: 1280px,
  xl: 1920px,
);

@mixin respond-to($breakpoint) {
  @media (min-width: map-get($breakpoints, $breakpoint)) {
    @content;
  }
}
```

---

## 5. 性能优化

### 5.1 代码分割

```javascript
// vite.config.js
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          "vue-vendor": ["vue", "vue-router", "pinia"],
          "element-plus": ["element-plus"],
          charts: ["echarts", "vue-echarts"],
          utils: ["axios", "dayjs", "markdown-it"],
        },
      },
    },
  },
});
```

### 5.2 懒加载

- 路由懒加载：`() => import('@/views/...')`
- 图表懒加载：使用时才导入 ECharts 组件
- 图片懒加载：`loading="lazy"`

### 5.3 缓存策略

- LocalStorage：Token、用户偏好
- SessionStorage：临时会话数据
- 浏览器缓存：静态资源 (1 年)

---

## 6. 构建与部署

### 6.1 开发环境

```bash
# 使用 pnpm
pnpm install
pnpm dev

# 使用 Bun
bun install
bun run dev:bun
```

### 6.2 生产构建

```bash
# 构建
pnpm build  # 或 bun run build:bun

# 输出目录
dist/
├── index.html
├── assets/
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── vendor-[hash].js
└── favicon.ico
```

### 6.3 部署方案

#### 方案 1: Nginx 静态托管

```nginx
server {
    listen 80;
    server_name idap.example.com;
    root /var/www/idap/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 方案 2: Docker

```dockerfile
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

---

## 7. 测试策略

### 7.1 单元测试

- 框架: Vitest
- 覆盖率: ≥ 70%
- 重点: Utils、Services、Stores

### 7.2 E2E 测试

- 框架: Playwright
- 场景: 登录、查询、对话、历史记录

---

## 8. 开发规范

### 8.1 代码风格

- 使用 `<script setup>` 语法
- 组件命名：PascalCase
- 文件命名：kebab-case
- 缩进：2 空格
- 分号：可选
- 引号：单引号

### 8.2 Commit 规范

```
feat: 新功能
fix: 修复 Bug
docs: 文档更新
style: 代码格式调整
refactor: 代码重构
test: 测试相关
chore: 构建/工具链更新
```

### 8.3 代码检查

```bash
# ESLint
pnpm lint

# Prettier
pnpm format
```

---

## 9. 浏览器兼容性

| 浏览器  | 最低版本 |
| ------- | -------- |
| Chrome  | 90+      |
| Firefox | 88+      |
| Safari  | 14+      |
| Edge    | 90+      |

**不支持**: IE 11 及以下
